const { db, ensureSchema } = require('../_lib/db');
module.exports=async function(req,res){if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});try{await ensureSchema();const sql=db();const rows=await sql`SELECT id,name,category,price,image,short_description,description,features,specifications FROM products WHERE published=true ORDER BY created_at DESC`;res.setHeader('Cache-Control','s-maxage=60, stale-while-revalidate=300');res.setHeader('Access-Control-Allow-Origin','*');res.json(rows)}catch(e){console.error(e);res.status(500).json({error:'Database unavailable'})}};
